// los Sets son un conjunto de valores, no pueden contener duplicados.
const list = new Set();

// .add() es un método que permite agregar un valor al Set que se añadió en ES6
list.add('item 1');
list.add('item 2').add('item 3');

console.log(list);