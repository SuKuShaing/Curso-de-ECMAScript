// En el ECMAScript 7 se agrega el operador ** para realizar la operación de exponenciación.
// antes se podía realizar con Math.pow(base, exponente) pero ahora es más sencillo.
const data = 3 ** 4;
console.log(data); // 81